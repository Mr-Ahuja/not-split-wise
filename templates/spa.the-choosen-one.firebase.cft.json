{
  "template": "spa.the-choosen-one.firebase",
  "version": "1.0.0",
  "extends": "templates/spa.the-choosen-one.cft.json#1.0.0",
  "requires": {
    "deploy.strategy": {
      "enum": ["firebase_hosting"],
      "bindings": {
        "firebase_hosting": { "operator": "op.deploy.firebase.hosting" }
      }
    }
  },
  "notes": [
    "Extends the-choosen-one SPA to allow Firebase Hosting with GitHub Actions CI."
  ]
}

